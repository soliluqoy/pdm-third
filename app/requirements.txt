# PREDICT — backend dependencies (lean: no MQTT, no Redis)
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29
pydantic>=2.7
pydantic-settings>=2.3
# Predictive maintenance: unsupervised anomaly detection (Isolation Forest)
scikit-learn>=1.4
numpy>=1.26
joblib>=1.3
